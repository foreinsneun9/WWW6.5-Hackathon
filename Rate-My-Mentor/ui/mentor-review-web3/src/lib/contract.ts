/**
 * 合约 ABI、地址（Day2 拿到合约后在此填写）
 * 与 wagmi `useReadContract` / `useWriteContract` 配合使用
 */

export const mentorRegistryAddress: `0x${string}` | "" = "";

/** 占位：替换为真实 ABI */
export const mentorRegistryAbi = [] as const;

export const companyRegistryAddress: `0x${string}` | "" = "";

export const companyRegistryAbi = [] as const;
